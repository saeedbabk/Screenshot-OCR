# Screenshot-OCR
This project extracts timestamps from security camera screenshots using a multi-engine OCR approach (EasyOCR, PaddleOCR, TrOCR, and if provided with Google Cloud key written on a *.json file, it can use Cloud Vision for maximum OCR capability).
